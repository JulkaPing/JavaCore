package classes.zoo;

public class ZooService {

    /**
     * Метод, который находит название зоопарка с самым большим количеством животных
     * @param zooparks
     * @return название зоопарка
     */
  /*  public String getZooManyAnimals(Zoopark[] zooparks) {
        int countAnimals = 0;
        String nameZoopark;
        for (Zoopark zoopark : zooparks ) {
            System.out.println(zoopark.getAnimals().length);
            if ( zooparks.length > countAnimals) {
                countAnimals = zooparks.length;
                nameZoopark = zoopark.getName();
            }
        }
        //return nameZoopark;
 //   }

    /**
     * Метод, который определяет среднюю стоимость билета из общего количества зоопарков
     * @param zooparks все зоопарки
     * @return средняя цена билета
     */
   /* public double price(Zoopark[] zooparks) {
        double averageTicketPrice = 0;
        double sum = 0;
            for (int i = 0; i < zooparks.length; i++) {
               sum = price(zooparks);

            }
        return sum;
    }*/






}
